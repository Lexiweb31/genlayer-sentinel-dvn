const stages=["Packet detected","RPC confirmations","GenLayer consensus","GenLayer finality","Signer quorum","LayerZero verification","OApp execution"],stageIndex={DETECTED:0,CONFIRMED:1,POLICY_PENDING:2,POLICY_FINALIZED:3,QUORUM_REACHED:4,VERIFIED:5,EXECUTED:6,REJECTED:3};
const timeline=document.querySelector("#timeline"),status=document.querySelector("#connection-status"),notice=document.querySelector("#live-notice");
function render(job){const current=stageIndex[job.stage]??0;timeline.replaceChildren(...stages.map((label,index)=>{const el=document.createElement("div");el.className=`step ${index<current?"complete":index===current?(job.stage==="REJECTED"?"rejected":"current"):""}`;const n=document.createElement("span");n.textContent=String(index+1).padStart(2,"0");const box=document.createElement("div"),strong=document.createElement("strong"),small=document.createElement("small");strong.textContent=label;small.textContent=index<current?"Complete":index===current?(job.stage==="REJECTED"?"Rejected":job.stage.replaceAll("_"," ").toLowerCase()):"Not started";box.append(strong,small);el.append(n,box);return el}));status.textContent=job.stage;status.className=`status ${job.stage==="REJECTED"?"bad":"live"}`;notice.textContent=`Live coordinator state · GUID ${job.packet.guid.slice(0,10)}…${job.packet.guid.slice(-8)}`}
function unavailable(message){render({stage:"DETECTED",packet:{guid:"0x0000000000000000"}});status.textContent="COORDINATOR UNAVAILABLE";status.className="status bad";notice.textContent=message}
async function refresh(){try{const response=await fetch("/api/jobs",{headers:{accept:"application/json"},cache:"no-store"});if(!response.ok)throw new Error(`status ${response.status}`);const jobs=await response.json();if(!Array.isArray(jobs)||jobs.length===0){status.textContent="NO PACKETS DETECTED";status.className="status";notice.textContent="Coordinator connected. No packet jobs exist.";return}render(jobs.at(-1))}catch(error){unavailable(`No coordinator data: ${error.message}. No simulated state is shown.`)}}
refresh();setInterval(refresh,5000);
