# The coordinator reads one isolated product-specific runtime manifest.
# Do not reuse another project's environment, RPC credentials, or signer keys.
SENTINEL_CONFIG_PATH=/absolute/path/to/private/sentinel-runtime.json
