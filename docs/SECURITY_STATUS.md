# Security verification status — 2026-08-09

`npm run check` is the required local gate for the 0.30.0 testnet prototype: it requires strict TypeScript, the pinned official GenVM linter, 24 official direct-runner Intelligent Contract tests, the full build, six Solidity sources compiled with solc 0.8.30 for Shanghai, dashboard validation, the ordinary Node suite, four serial fixed-seed contract property campaigns, and closed Slither analysis. Production-source Slither enforcement remains High 0, Medium 0, reviewed Low 1, and reviewed Informational 6; fully validated dependency-only and mixed-result counts are reported separately.

Latest gate evidence (2026-08-09): typecheck, GenVM lint, all 24 Intelligent Contract cases, compilation, dashboard checks, and all 575 ordinary Node cases passed with zero failures, skips or todos. The focused pathway/readiness/dashboard gate separately passed 207 cases with zero failures, skips or todos. All four fixed-seed property campaigns passed (112 generated runs). Slither 0.11.5 analyzed both production targets with solc 0.8.30 and reported High 0, Medium 0, reviewed Low 1, and reviewed Informational 6. This is fresh local verification of the current milestone branch, not evidence of deployment, live-chain behavior, third-party audit or production readiness.

The 0.29.0 keyless readiness boundary accepts one closed-schema canonical public manifest and reads only the fixed repository-local readiness configuration, network metadata, dated audit evidence, generated build manifest, two generated production artifacts, two production sources, and sanitized Git results. It runs Git with argument arrays, no shell, an allowlisted child environment, optional locks disabled, and a closed allowlist for repository-local Git configuration; it never forwards ambient variables or permits configured external helpers. A Git preflight runs before any compilation, the trusted compiled command reruns the pinned local compiler in process, and a second Git inspection binds the post-evidence state. Its in-memory production provenance must exactly agree with source bytes, generated artifacts, the ignored build manifest, and the public expectations. It binds the current commit, dirty state, source hashes, compiler settings, independently compiled ABI/creation-bytecode hashes, network metadata, audit digests, and audit age. Public output is canonical, allowlisted, always unsigned/not deployed/not verified, separately approval-gated, and contains no transactions. Tests cover invalid argument shapes, duplicate/noncanonical JSON across public and local evidence, secret-shaped fields, synchronized public/build/artifact forgery, generated-artifact drift, trusted compiler-output agreement, Git-preflight ordering, hostile Git helpers, special-file rejection, sanitized dependency failures, prohibited-capability getter traps, ambient secret-like variables, artifact/network/conformance/configuration precedence, and exclusive mode-`0600` no-overwrite output with handle closure and temporary-file cleanup.

This is local provenance evidence, not a security audit or chain observation. The generated `dist/contracts/build-manifest.json` and artifact files are ignored build output; the command detects their disagreement with its fresh in-memory compilation, but Git does not independently preserve them. A compromised compiler package/runtime or workstation remains in scope. Git binding applies only to the current worktree and cannot establish reviewer identity, branch protection, remote publication, or reproducible builds elsewhere. The dated LayerZero metadata can become stale, and the current audit proves contract metadata only; it does not validate the live directional pathway. A well-formed malicious manifest cannot obtain wallet/signing/deployment capability from this command, but its public owner/operator choices are not thereby trusted or approved.

The M2 pathway-auditor boundary accepts one canonical public manifest and reads only the fixed auditor policy, network metadata, dated official-source evidence, provider/DVN-operator registries, compiled build manifest, and two production contract artifacts. It constructs exactly two restricted clients per chain and permits only `eth_chainId`, `eth_blockNumber`, `eth_getBlockByNumber`, `eth_getCode`, `eth_call`, `eth_getTransactionByHash`, and `eth_getTransactionReceipt`. It rejects credentials, custom ports, redirects, retries, unsafe DNS answers, response ambiguity, noncanonical inputs, secret-shaped fields, unexpected methods/parameters, unstable blocks, provider transcript disagreement, and existing output files. State reads are pinned by EIP-1898 canonical block hash. The artifact parser recomputes the outer evidence digest, configuration digest, provider-result digests, derived status, and completeness invariants.

The command records transport agreement and reviewed operator independence separately. `config/rpc-provider-audit.json` currently contains no reviewed operators, official runtime-code hashes remain unpinned, no deployment record exists, LayerZero DVN operators are unreviewed, and confirmation values remain `UNAPPROVED_PROJECT_POLICY`; therefore no current live artifact can honestly clear all gates. A null deployment yields explicit blockers and null configuration rather than fixture state. Readiness may bind one exact consistent artifact but cannot mutate or infer `livePathwayValidated`, DVN selection, confirmation approval, onboarding, GenLayer finality, signer/recovery independence, or deployment security approval.

Adversarial coverage includes malicious manifest shapes, secret fields, DNS rebinding/private ranges, method/parameter confusion, JSON-RPC correlation, size/time/header limits, chain/header disagreement, block instability, provider transcript drift, missing/code-identity evidence, creation transaction/receipt/constructor/runtime mismatches, default/inherited ULN values, peer/Executor/DVN/order/threshold/Dead-DVN/optional-Sentinel/adapter/signer drift, canonical artifact mutation, dashboard tampering, readiness digest drift, and prohibited-capability getter traps. This remains local test evidence. Live smoke: **NOT RUN — PUBLIC RPC INPUTS NOT SUPPLIED**.

The operations dashboard now includes an operations-first Sentinel hero and a strict local file inspector. It begins at `NOT OBSERVED`, reads an operator-selected artifact only in browser memory, performs no upload or persistence, and does not merge audit evidence with coordinator packet, GenLayer, signer, delivery, or execution state. Video, poster, fonts, and social preview are self-hosted with recorded provenance; the hosted static package passes its local worker/package tests but is not published and has no live URL.

The contract-assurance gate uses fast-check `4.9.0` for 112 generated runs across adapter 3-of-5 authorization/atomicity and OApp execution/replay/rejection/rollback campaigns. Each case is isolated by an EVM snapshot. Deliberately weakened quorum and replay predicates failed on their first generated cases and shrank to minimal counterexamples. Slither `0.11.5` runs separately against `SentinelDVNAdapter.sol` and `TreasuryPolicyOApp.sol` with a checksum-pinned native solc `0.8.30`, Shanghai, optimizer 200, and ignored findings shown. The final result is High 0, Medium 0, reviewed Low 1 and reviewed Informational 6. Exact findings are bound to description and source bytes; stale, broad, unused and expired entries fail, and High/Medium findings cannot be allowlisted or hidden by an inline suppression.

The zero-fee adapter now explicitly inherits the pinned official `ILayerZeroDVN`; `assignJob` has the required payable ABI but rejects every nonzero `msg.value`, and tests prove exact refusal precedence and no value retention through that entry point. A separate nonreentrant, permissionless recovery call can return force-sent native balance only to the immutable authorized message library; callers cannot redirect it. OApp actions still require zero native value during quote/send/receive, the catch-all payable receive hook remains absent, signer locals are explicit, and adapter submission plus OApp sending are nonreentrant. The current interface and readiness evidence is recorded in [`research/2026-08-02-layerzero-interface-conformance-audit.md`](research/2026-08-02-layerzero-interface-conformance-audit.md); the original property seeds and assurance limits remain in the dated [`research/2026-07-29-contract-assurance-audit.md`](research/2026-07-29-contract-assurance-audit.md), while current exact finding fingerprints live in `config/slither-allowlist.json`.

The Intelligent Contract suite runs `genlayer-test==0.29.2` with GenVM `v0.2.16`, strict mocks, serialization/pickling checks, warnings as errors, deterministic warped time and a repository-local SDK cache. It validates coordinator authorization, constructor and byte-bound checks, credential-free HTTPS, immutable GUID records, evidence digest before LLM execution, JSON framing, fail-closed web/LLM and answer handling, structured audit fields, deterministic timestamps, and action/policy/request digests. Captured-validator cases accept identical controlled decision/authorization and reject changed decision, changed authorization with the same `ALLOW`, and changed renderer evidence. Because the pinned direct WASI mock lacks the comparative host call, those validator cases use a test-only equality hook; they do not prove the real NLP comparator or decentralized consensus.

The contract stores `DECIDED`, never `FINALIZED`. A strict TypeScript compatibility decoder recomputes the same domain-separated request binding before the established finality adapter can return a decision. Python and TypeScript independently produce the exact vector `0xe8539dc6d81fbd8491d86ca707cccc0d0e3a91629565eda34e7e1b5a85693b42`; malformed, oversized, packet/evidence-mismatched and differently bound records are rejected. Signing requires exact GenLayer transaction correlation plus `FINALIZED`/`7`, successful exact `evaluate` execution and the latest strict record.

The 0.27.0 signer boundary accepts only canonical `sentinel-signer/v2`. The coordinator derives a bounded transaction/evidence/action/policy witness from durable state; every signer-side attestor independently checks the pinned policy recipient, six exact call arguments, successful execution, immutable record binding, decision, policy version and reason code before key invocation. The durable replay database binds both execution and authorization digests and refuses populated v1 state without an explicit operator migration. Tests prove v1 rejection, strict UTF-8/request limits, sanitized dependency failures, 3-of-5 success with two independent refusals, 2-of-5 failure without adapter submission, exclusion of a mismatched transaction view, DENY with zero additional key/destination activity, and conflicting authorization after replay-store reopen.

The 0.28.0 signer transport adds 18 mTLS tests, including real-socket coverage around native Node TLS 1.3 mutual authentication. They prove trusted client/server chains, logical hostname/SNI, both live canonical-DER SPKI pins, strict method/path/media/encoding/header framing, 32,768-byte request and 16,384-byte response limits, one bounded client timeout, sanitized parser/abort/oversize failures, single-lifecycle graceful stop and drain expiry. The complete E2E reaches `RemoteSignerClient`, native transport, daemon, v2 handler, real SQLite replay, fixture finality and one ephemeral signer key. A CA-trusted coordinator with the wrong application pin reaches TLS but receives 401 before replay/finality/key; changed authorization conflicts after store reopen; a wrong signer pin fails before handler contact. Test certificates exist only in temporary directories and are untracked.

The established suite still covers canonical packet, policy, signer, adapter, OApp, listener, recovery, destination-outbox, runtime and read-only API invariants. It requires exact GenLayer JSON-RPC correlation and the strict `FINALIZED`/`7`/successful-execution/`latest-final` gate; two-provider source and destination agreement; pinned Endpoint, receive-library, ULN302, OApp and adapter configuration; persist-before-sign and persist-before-submit ordering; exact cryptographic 3-of-5 DVN and recovery quorums; expiry at every material boundary; no automatic rebroadcast after an ambiguous production attempt; independent receipt/event/used-state confirmation; bounded ingestion quarantine; and evidence-bound packet requeue.

The 0.25.0 recovery coverage validates five sorted recovery identities separate from the five DVN signers, quorum three, a 900-second minimum delay, a maximum one-day proposal lifetime, deployment-domain binding and strict canonical proposal fields. Real EIP-712 tests reject insufficient, duplicate, unordered, unauthorized, wrong-domain and altered approvals; premature, expired and cross-deployment proposals; and invalid ingestion/destination shapes. SQLite lease tests prove explicit runtime/recovery exclusion, owner and timestamp fencing, controlled stale takeover for runtime owners only, no stale bypass for recovery, and clean HTTP → lease → store shutdown ordering. Composition fails closed when another live runtime owns the database.

Every protected recovery mutation writes one ABI-hashed receipt in the same immediate transaction. Tests prove idempotent replay, cross-restart chain continuity, tamper detection, exact dead-letter deletion, the dedicated `RECOVERY_REQUIRED → CONFIRMED` transition, preservation of the signed envelope/shares, and rollback of both state and audit on mismatch. Operator-service tests repeat canonical source receipt plus historical pathway proof before requeue, repeat destination pathway plus receipt/block/event/used/confirmation proof before confirmation, bind complete retained state through ABI preconditions, and never expose a submitter. The CLI tests require absolute explicit files, accept detached bundles only, request no key/wallet/signer capability, close resources on every path, and emit only allowlisted codes. The status/dashboard tests expose only sanitized read-only receipt fields and reject mutation surfaces or simulated recovery records.

The adversarial E2E deploys the real local `TreasuryPolicyOApp`, `SentinelDVNAdapter`, verification target and action target; collects a real DVN 3-of-5 quorum; broadcasts one valid adapter transaction; forces the submitter to return ambiguously; verifies the persisted recovery-required state; obtains three signatures from five distinct recovery wallets; waits the injected 900-second timelock; repeats real local pathway and receipt proof; confirms without a second adapter submission; and then completes the ordinary destination OApp execution and target mutation. Reapplying the same bundle returns the same receipt and leaves submission count and audit count at one.

The 0.24.0 coverage additionally proves the complete explicit source pathway at the packet's exact historical block: chain and block hash agreement, code presence, explicit non-default SendUln302 selection, supported destination EID, raw OApp ULN configuration, executor/message-size configuration, peer binding and every configured DVN. It exercises every pinned-field drift, provider disagreement, unsafe origin and sanitized transport failure. The combined verifier refuses GenLayer submission when either canonical receipt verification or historical pathway verification fails, requires at least two receipt records, attaches one shared nonzero configuration digest to both, persists it through the state machine and renders it as deterministic `source config` evidence in the dashboard. Composition tests prove the injected source transport is used. Destination tests separately reject a merged matching receive configuration when its raw app values are inherited from defaults.

The new local-app coverage validates a strict loopback-only public capability; serialized injected-wallet connection; chain, selected-account and on-chain OApp-owner checks; rejection of owners controlled by the harness's unlocked account set; fee quote invalidation and stale-result suppression; one ordinary `eth_sendTransaction`; pre-broadcast failure recovery; exact successful receipt and `ActionSent` GUID extraction; no wallet secret or signing-method request; preservation of the submitted hash/GUID after an account or chain change; and a one-source-action terminal lock. The end-to-end tests carry that exact mined packet through separately labeled local packet and receipt checks, a pending-then-final `LOCAL_POLICY_FIXTURE`, five real ephemeral ECDSA shares, on-chain 3-of-5 adapter verification, destination OApp execution and target mutation. An altered action finalizes `DENY` before signer or destination activity.

A post-mining coordinator/store restart rediscovers the packet without resending the wallet transaction. Separate recovery tests prove that an ambiguous executor attempt is reserved once, a stable incident survives closing and reopening the real SQLite store, the delivery API/timeline exposes OApp-execution reconciliation instead of claiming success, and the incident clears durably only after the executed GUID and exact target mutation are observed. The confirmer-level ambiguity test uses a shared attempt-store double across instances to prove one broadcast; it is not mislabeled as a full harness restart after broadcast.

This does **not** cover a deployed public coordinator or signer daemon, an approved account provider, or an official live GenLayer transaction-witness reader; live EndpointV2/ULN302 calls; proof that configured RPC origins are independently operated; atomic state proofs across source or destination path reads; live-chain receipt/reorg polling; destination nonce, fee or replacement management; a deployed recovery ceremony; on-chain recovery governance; HSM-enforced operator policy; independent recovery-operator failure domains; external audit-head anchoring; distributed exactly-once delivery; retry backoff; Studio/Bradbury compatibility; the real GenLayer semantic comparator; live validator/model diversity; live web-render stability; live GenLayer finality and appeal behavior; multi-node coordinator storage; production CA ownership, issuance, rotation, revocation or OCSP; key custody; five independent DVN signer failure domains; rate limiting/ingress protection; broader stateful/sequence fuzzing; formal verification; or a third-party audit. The four fixed-seed property campaigns and detector-based Slither result are bounded local evidence, not substitutes for those missing controls. `MockEndpointV2`, `MockVerificationTarget`, the local EDR proof sources, runtime TLS certificates and `LOCAL_POLICY_FIXTURE` are explicitly test fixtures, not security models of LayerZero, independent RPC infrastructure, production PKI or GenLayer consensus. Direct-mode execution is stronger than syntax checking but remains controlled local test evidence.

Registry evidence refreshed with Corepack npm `10.9.2` on 2026-07-29:

- `npm audit --omit=dev` reports **0 vulnerabilities**.
- `npm ls --omit=dev --depth=2` exits zero and exposes only Sentinel's pinned ethers 6 runtime subtree.
- The complete development tree reports 19 vulnerabilities: 13 low, 2 moderate, 4 high and 0 critical.

The development findings remain in Hardhat `3.10.0` through `adm-zip`, the local `solc` wrapper through `tmp`, and LayerZero's development-only ethers 5 tree through packages including `elliptic` and `ws`. These tools are not shipped in the coordinator runtime, but that distinction does not waive the findings. Hardhat is restricted to reviewed local test artifacts and a loopback EDR server, and compiler input remains limited to reviewed repository sources. `npm audit` proposes breaking or semantically unsafe forced changes, including Hardhat `0.0.7` and solc `0.5.0`; do not run `npm audit fix` blindly.

The npm lock remains pinned to npm 10.9.2. GenLayerJS is absent from production dependencies: Sentinel owns the small read-only status protocol and keeps account-aware submission/contract reads behind an injected interface. The wallet bundle is built locally with pinned esbuild `0.28.1`; it uses no CDN, inline script, dynamic import, raw transaction, signing secret or remote asset. The GenLayer and assurance Python graphs are separate, hash-locked and repository-local. Contract hardening changed both production bytecodes as documented; no deployed bytecode exists and no migration is claimed.

Residual local-demo risks are documented, not hidden: loopback is not authentication on a compromised host; EDR wallets/signers are not independent operators; same-tab reload restores only a public capability-bound transaction/GUID locator and no wallet connection, quote, approval, policy, signer, or execution authority; a user must reconcile an ambiguous source transaction manually; the local executor fails closed and has no authenticated manual reconciliation workflow, although a later authoritative on-chain execution observation clears its incident; scheduler failures lack production alerting; and stopping the harness destroys its temporary chain and database. GenLayer toolchain evidence and gates are in [`research/2026-07-28-genlayer-direct-mode-audit.md`](research/2026-07-28-genlayer-direct-mode-audit.md); the npm dependency evidence and upgrade gates remain in [`research/2026-07-22-dependency-audit.md`](research/2026-07-22-dependency-audit.md).
