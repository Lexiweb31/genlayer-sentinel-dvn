# Security verification status — 2026-07-25

`npm run check` exits zero for version `0.25.0`: it compiles five Solidity sources with solc 0.8.30 (Shanghai target), strictly type-checks TypeScript, syntax-checks the Intelligent Contract safety boundary, builds and statically checks the self-hosted wallet bundle, and passes 195 tests with zero failures, skips, or todos.

The established suite still covers canonical packet, policy, signer, adapter, OApp, listener, recovery, destination-outbox, runtime and read-only API invariants. It requires exact GenLayer JSON-RPC correlation and the strict `FINALIZED`/`7`/successful-execution/`latest-final` gate; two-provider source and destination agreement; pinned Endpoint, receive-library, ULN302, OApp and adapter configuration; persist-before-sign and persist-before-submit ordering; exact cryptographic 3-of-5 DVN and recovery quorums; expiry at every material boundary; no automatic rebroadcast after an ambiguous production attempt; independent receipt/event/used-state confirmation; bounded ingestion quarantine; and evidence-bound packet requeue.

The 0.25.0 recovery coverage validates five sorted recovery identities separate from the five DVN signers, quorum three, a 900-second minimum delay, a maximum one-day proposal lifetime, deployment-domain binding and strict canonical proposal fields. Real EIP-712 tests reject insufficient, duplicate, unordered, unauthorized, wrong-domain and altered approvals; premature, expired and cross-deployment proposals; and invalid ingestion/destination shapes. SQLite lease tests prove explicit runtime/recovery exclusion, owner and timestamp fencing, controlled stale takeover for runtime owners only, no stale bypass for recovery, and clean HTTP → lease → store shutdown ordering. Composition fails closed when another live runtime owns the database.

Every protected recovery mutation writes one ABI-hashed receipt in the same immediate transaction. Tests prove idempotent replay, cross-restart chain continuity, tamper detection, exact dead-letter deletion, the dedicated `RECOVERY_REQUIRED → CONFIRMED` transition, preservation of the signed envelope/shares, and rollback of both state and audit on mismatch. Operator-service tests repeat canonical source receipt plus historical pathway proof before requeue, repeat destination pathway plus receipt/block/event/used/confirmation proof before confirmation, bind complete retained state through ABI preconditions, and never expose a submitter. The CLI tests require absolute explicit files, accept detached bundles only, request no key/wallet/signer capability, close resources on every path, and emit only allowlisted codes. The status/dashboard tests expose only sanitized read-only receipt fields and reject mutation surfaces or simulated recovery records.

The adversarial E2E deploys the real local `TreasuryPolicyOApp`, `SentinelDVNAdapter`, verification target and action target; collects a real DVN 3-of-5 quorum; broadcasts one valid adapter transaction; forces the submitter to return ambiguously; verifies the persisted recovery-required state; obtains three signatures from five distinct recovery wallets; waits the injected 900-second timelock; repeats real local pathway and receipt proof; confirms without a second adapter submission; and then completes the ordinary destination OApp execution and target mutation. Reapplying the same bundle returns the same receipt and leaves submission count and audit count at one.

The 0.24.0 coverage additionally proves the complete explicit source pathway at the packet's exact historical block: chain and block hash agreement, code presence, explicit non-default SendUln302 selection, supported destination EID, raw OApp ULN configuration, executor/message-size configuration, peer binding and every configured DVN. It exercises every pinned-field drift, provider disagreement, unsafe origin and sanitized transport failure. The combined verifier refuses GenLayer submission when either canonical receipt verification or historical pathway verification fails, requires at least two receipt records, attaches one shared nonzero configuration digest to both, persists it through the state machine and renders it as deterministic `source config` evidence in the dashboard. Composition tests prove the injected source transport is used. Destination tests separately reject a merged matching receive configuration when its raw app values are inherited from defaults.

The new local-app coverage validates a strict loopback-only public capability; serialized injected-wallet connection; chain, selected-account and on-chain OApp-owner checks; rejection of owners controlled by the harness's unlocked account set; fee quote invalidation and stale-result suppression; one ordinary `eth_sendTransaction`; pre-broadcast failure recovery; exact successful receipt and `ActionSent` GUID extraction; no wallet secret or signing-method request; preservation of the submitted hash/GUID after an account or chain change; and a one-source-action terminal lock. The end-to-end tests carry that exact mined packet through separately labeled local packet and receipt checks, a pending-then-final `LOCAL_POLICY_FIXTURE`, five real ephemeral ECDSA shares, on-chain 3-of-5 adapter verification, destination OApp execution and target mutation. An altered action finalizes `DENY` before signer or destination activity.

A post-mining coordinator/store restart rediscovers the packet without resending the wallet transaction. Separate recovery tests prove that an ambiguous executor attempt is reserved once, a stable incident survives closing and reopening the real SQLite store, the delivery API/timeline exposes OApp-execution reconciliation instead of claiming success, and the incident clears durably only after the executed GUID and exact target mutation are observed. The confirmer-level ambiguity test uses a shared attempt-store double across instances to prove one broadcast; it is not mislabeled as a full harness restart after broadcast.

This does **not** cover a standalone production daemon or approved account provider; live EndpointV2/ULN302 calls; proof that configured RPC origins are independently operated; atomic state proofs across source or destination path reads; live-chain receipt/reorg polling; destination nonce, fee or replacement management; a deployed recovery ceremony; on-chain recovery governance; HSM-enforced operator policy; independent recovery-operator failure domains; external audit-head anchoring; distributed exactly-once delivery; retry backoff; live GenLayer testnet compatibility; validator variance and web-render stability; multi-node coordinator storage; production mutual TLS/certificate lifecycle; key custody; five independent DVN signer failure domains; rate limiting; fuzzing; Solidity static analysis; formal verification or third-party audit. `MockEndpointV2`, `MockVerificationTarget`, the local EDR proof sources and `LOCAL_POLICY_FIXTURE` are explicitly test fixtures, not security models of LayerZero, independent RPC infrastructure or GenLayer consensus. The Intelligent Contract checker is syntax/guardrail validation, not GenVM execution.

Registry evidence collected on 2026-07-25:

- `npm audit --omit=dev` reports **0 vulnerabilities**.
- `npm ls --omit=dev --depth=2` exits zero and exposes only Sentinel's pinned ethers 6 runtime subtree.
- The complete development tree reports 19 vulnerabilities: 13 low, 2 moderate, 4 high and 0 critical.

The development findings remain in Hardhat `3.10.0` through `adm-zip`, the local `solc` wrapper through `tmp`, and LayerZero's development-only ethers 5 tree through packages including `elliptic` and `ws`. These tools are not shipped in the coordinator runtime, but that distinction does not waive the findings. Hardhat is restricted to reviewed local test artifacts and a loopback EDR server, and compiler input remains limited to reviewed repository sources. `npm audit` proposes breaking or semantically unsafe forced changes, including Hardhat `0.0.7` and solc `0.5.0`; do not run `npm audit fix` blindly.

The lock root is version `0.25.0` and remains pinned to npm 10.9.2. The 0.25.0 lockfile changes only root version metadata; the dependency graph is unchanged from the audited 0.24.0 tree. GenLayerJS is absent from production dependencies: Sentinel owns the small read-only status protocol and keeps account-aware submission/contract reads behind an injected interface. The wallet bundle is built locally with pinned esbuild `0.28.1`; it uses no CDN, inline script, dynamic import, raw transaction, signing secret or remote asset. This milestone adds the recovery policy/domain, detached proposal verification, runtime lease, audit ledger, protected store transitions, operator service, keyless CLI, read-only API/dashboard evidence and local recovery E2E. Solidity production sources and dependency versions are unchanged.

Residual local-demo risks are documented, not hidden: loopback is not authentication on a compromised host; EDR wallets/signers are not independent operators; the browser does not restore its submitted hash/GUID after a full reload; a user must reconcile an ambiguous source transaction manually; the local executor fails closed and has no authenticated manual reconciliation workflow, although a later authoritative on-chain execution observation clears its incident; scheduler failures lack production alerting; and stopping the harness destroys its temporary chain and database. The complete dependency evidence and upgrade gates remain in [`research/2026-07-22-dependency-audit.md`](research/2026-07-22-dependency-audit.md).
