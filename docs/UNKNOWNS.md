# External confirmation register

| Question | Owner | Current safe behavior |
|---|---|---|
| Does Essence retry a failed or unavailable extra-context request, at what cadence/backoff, which documented response shape is authoritative, and can pending be represented? | LayerZero | The API reference shows `{ "valid": boolean }`; the implementation guide requires a bare boolean. Neither defines pending or retry cadence. Keep async GenLayer decisions in Sentinel's explicit coordinator and do not claim native Gasolina integration. |
| Does the official DVN boundary require payable `assignJob`, and can Sentinel remain zero-fee? | LayerZero documentation + repository evidence | Closed on 2026-08-02: the pinned official interface is payable, and pinned `SendUlnBase` calls it without value. Sentinel explicitly implements the interface and rejects every nonzero `msg.value`; force-sent balance can return only to the immutable authorized message library. This closes only interface mutability and local value handling, not DVN topology or onboarding. |
| Will LayerZero onboard this testnet DVN, what authenticated ingress/caller contract is required, and what VID will its deployed contracts use? | LayerZero | The guide requires authenticated ingress and documents URL handoff → `/signer-info` → LayerZero DVN deployment/address return with Essence initially holding `ADMIN_ROLE`. Treat Sentinel as an interface-conformant adapter prototype until LayerZero accepts the integration and returns addresses. |
| Which independent operators and exact directional ULN302 configuration should protect the Sepolia → Arbitrum Sepolia OApps at deployment time? | LayerZero + security review | Protocol libraries and active provider listings were audited on 2026-07-22; do not infer resolved pathway defaults. Query both sides, select diverse providers, sort addresses, reject Dead DVNs and pin matching confirmations. |
| What are the independently reviewed runtime-code Keccak-256 identities for the current Ethereum Sepolia EndpointV2, SendUln302 and Executor and Arbitrum Sepolia EndpointV2 and ReceiveUln302? | LayerZero primary-source review + reproducible chain evidence | Unresolved. `config/pathway-auditor.json` keeps all five expectations `null`; the auditor may record code presence but must emit `AUDIT_CODE_IDENTITY_UNPROVEN` until exact reviewed hashes are committed. |
| Which four credential-free public RPC transports are approved, and are their two-per-chain operator families genuinely independent? | Security/infrastructure review | Unresolved. `config/rpc-provider-audit.json` records no reviewed providers. Different URLs, IPs or brands do not prove independent ownership or failure domains; no live smoke is run without operator-supplied public inputs and explicit authorization. |
| What complete public deployment record should the one-way pathway audit? | Deployment owner + independent reviewer | Unresolved because no deployment exists. Keep `deployment:null`. A future complete record requires both OApps, both adapters, all four creation transaction hashes, both OApp delegates, five sorted authorized signers and quorum three, backed by matching creation/runtime evidence from both providers. |
| Has one immutable live pathway artifact reached `OBSERVED_PATHWAY_CONSISTENT`, and has a human approved what it means? | Security review + operator | No. Live smoke is `NOT RUN — PUBLIC RPC INPUTS NOT SUPPLIED`. Even a future consistent artifact cannot set readiness `livePathwayValidated`, approve confirmations, authorize deployment, or establish GenLayer semantic finality automatically. |
| Does `SentinelPolicy` execute in the current pinned GenVM direct-mode runtime? | Repository evidence | Closed for local direct mode on 2026-07-28: `genlayer-test==0.29.2`, `genvm-linter==0.11.0`, GenVM `v0.2.16`, strict mocks and pickling pass 24 contract tests. This is not a live-network or consensus result. |
| Does the exact contract execute unchanged in current Studio and Bradbury with multiple real validators? | GenLayer | Unresolved. Do not enable account-aware submission or signing from this evidence; record deployed bytecode/source identity and multi-validator results first. |
| Are external web responses pinned/reproducible across live GenLayer validators for the chosen governance endpoint? | GenLayer | Unresolved. Direct tests use controlled renderer results. Bind the rendered digest, fail closed on disagreement/staleness, and measure live renderer variance before deployment. |
| Does the live equivalence comparator reliably reject changed authorization even when validators return the same decision token? | GenLayer + security review | Unresolved. Direct-mode re-execution uses a test-only equality hook because the pinned direct WASI mock lacks the comparative host call. Exercise the real comparator with model and validator diversity. |
| What exact Bradbury transaction status, appeal and finality sequence should the coordinator consume, and with what latency budget? | GenLayer | Unresolved. Retain exact correlation plus `FINALIZED`/`7`/successful-execution/`latest-final`; fail closed on every other state until confirmed against live transactions. |
| Which official GenLayer SDK/RPC response is the stable source for signer-side transaction recipient, method, six decoded arguments and execution result, and how should it be authenticated? | GenLayer | Partially reduced on 2026-08-11: the keyless official-RPC receipt transport adapter validates `FINALIZED`/7, transaction ID, recipient, raw GenVM `txCallData` and execution result using `gen_getTransactionStatus` and `gen_getTransactionReceipt`. A separate closed-schema finality-source review record now pins only a source fingerprint, Bradbury chain ID, policy-contract identity, `latest-final` mode and `UNAPPROVED` codec state; it contains no URL and cannot authorize a signer. It cannot safely decode the raw call into Sentinel's required method and six arguments. Keep `GenLayerSignerWitnessReader` injected and refuse production signer deployment until a reviewed live codec and latest-final record adapter reproduce the v2 witness checks against finalized Bradbury transactions. |
| Who owns the production signer/coordinator CAs, issuance, rotation, revocation, OCSP policy and SPKI-pin ceremony? | Security review + signer operators | Unresolved. The native daemon accepts only injected in-memory capabilities and requires a new instance for rotation. Runtime-generated test certificates prove local behavior only; do not expose a public signer until an audited multi-operator PKI ceremony and emergency revocation runbook exist. |
| What production model diversity and validator independence are available and observable for this policy? | GenLayer + security review | Unresolved. Do not infer decentralization from direct mode or endpoint count; require documented independent failure domains and adversarial results. |
| Are 15/64 confirmations appropriate for the production candidate? | Security review | Values are placeholders, never production claims. |
| Is the operations-first dashboard publicly deployed at an approved live URL? | Publication owner | No. The self-hosted static package and local worker tests exist, but publication and any live URL require separate explicit approval and successful deployment evidence. |
