# Milestones

- M0 (this repository): audited architecture, state machine, policy record IC, adapter/OApp contracts, quorum abstraction, fixture-based tests, honest dashboard shell.
- M1 (wallet-originated local slice complete; live integration pending): pinned LayerZero compilation; strict treasury-action request assembly; capability-gated injected-wallet quote/send/receipt; exact mined GUID correlation; canonical PacketV1 and receipt verification; explicitly labeled deterministic governance evidence and `LOCAL_POLICY_FIXTURE` pending/final allow/deny; real local 3-of-5 quorum over five ephemeral ECDSA identities; pinned adapter/path verification; canonical ULN302 intent; persist-before-sign and persist-before-submit recovery; real adapter `Verified`/used-state confirmation; real destination OApp execution; visible durable local-executor reconciliation; denial before signing; acknowledged at-least-once ingestion; coordinator/store restart without wallet resend; supervised production composition; and an honest same-origin action/operations app are complete. EDR, semantic finality, proof origins, executor, funding, and signers remain fixtures. Approved GenLayer and destination account-provider construction, production mutual TLS, five independent signer operators, remaining development advisory resolution, live directional ULN302 validation, deployment, and GenLayer Studio/direct-mode tests remain.
- M2: user-approved funded one-way Sepolia → Arbitrum Sepolia deployment, independent DVN config, live event ingestion, real finalized GenLayer read.
- M3: five isolated signer processes across distinct operators/failure domains, 3-of-5 quorum, monitoring, runbooks, adversarial E2E.
- M4: LayerZero/GenLayer confirmations resolved, external audit, design partner pilot. Mainnet is a separate decision.
