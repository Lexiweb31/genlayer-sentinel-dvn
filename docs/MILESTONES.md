# Milestones

- M0 (this repository): audited architecture, state machine, policy record IC, adapter/OApp contracts, quorum abstraction, fixture-based tests, honest dashboard shell.
- M1 (local backend slice complete; operational integration in progress): pinned LayerZero compilation, canonical PacketV1 verification, strict treasury-action request assembly, local adapter/OApp lifecycle on an isolated Hardhat EDR runner, exact `gen_getTransactionStatus` finality validation, `latest-final` policy binding, a pinned destination-path verifier, canonical ULN302 verification intent, persist-before-sign and persist-before-submit recovery, authenticated remote signer protocol, local 3-of-5 quorum over five real ephemeral ECDSA identities, independent receipt/event/replay confirmation, acknowledged at-least-once ingestion, supervised four-phase runtime composition and an honest read-only operations app are complete. Approved GenLayer and destination account-provider construction, production mutual TLS, five independent signer operators, remaining development advisory resolution, live directional ULN302 validation and GenLayer Studio/direct-mode tests remain.
- M2: user-approved funded one-way Sepolia → Arbitrum Sepolia deployment, independent DVN config, live event ingestion, real finalized GenLayer read.
- M3: five isolated signer processes across distinct operators/failure domains, 3-of-5 quorum, monitoring, runbooks, adversarial E2E.
- M4: LayerZero/GenLayer confirmations resolved, external audit, design partner pilot. Mainnet is a separate decision.
