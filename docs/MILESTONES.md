# Milestones

- M0 (this repository): audited architecture, state machine, policy record IC, adapter/OApp contracts, quorum abstraction, fixture-based tests, honest dashboard shell.
- M1 (wallet-originated local slice, production-path verification, GenLayer direct-mode conformance, independent-signer finality boundary and hardened off-chain recovery code complete; live integration pending): pinned LayerZero compilation; strict treasury-action request assembly; capability-gated injected-wallet quote/send/receipt; exact mined GUID correlation; canonical PacketV1 and receipt verification; exact-packet-block source Endpoint/send-library/OApp/peer/executor/raw-ULN verification through two injected RPC transports; a persisted and dashboard-visible historical configuration digest; explicit rejection of default source libraries and inherited destination ULN values; explicitly labeled deterministic governance evidence and `LOCAL_POLICY_FIXTURE` pending/final allow/deny; real local 3-of-5 quorum over five ephemeral ECDSA identities; pinned adapter/path verification; canonical ULN302 intent; persist-before-sign and persist-before-submit recovery; real adapter `Verified`/used-state confirmation; real destination OApp execution; visible durable local-executor reconciliation; denial before signing; acknowledged at-least-once ingestion; coordinator/store restart without wallet resend; supervised production composition; and an honest same-origin action/operations app are complete. The Intelligent Contract additionally passes pinned official GenVM lint and direct execution with strict mocks, pickling, semantic fail closure, controlled validator variance, immutable structured records, and an exact Python/TypeScript request-binding vector. `sentinel-signer/v2` carries a bounded durable GenLayer authorization witness; signer-side attestors independently require the exact finalized transaction call and strict record, while replay storage binds execution and authorization digests. Adversarial tests prove 3-of-5 with two refusals, 2-of-5 fail closure, mismatched signer transaction exclusion and replay conflict after restart. Recovery adds a separate 3-of-5 EIP-712 council, deployment binding, 15-minute minimum timelock, explicit runtime fencing, repeated deterministic proof, atomic protected transitions, hash-chained receipts, a keyless offline CLI and read-only audit dashboard. The adversarial local E2E broadcasts one valid adapter transaction, forces an ambiguous submitter response, recovers it without rebroadcast, and completes the real destination OApp action. EDR, semantic finality, transaction readers, proof origins, executor, funding, DVN signers and recovery operators remain fixtures. An official live GenLayer reader, approved GenLayer and destination account-provider construction, Studio/Bradbury multi-validator compatibility, live GenLayer finality, a listening production mutual-TLS signer daemon, five independent DVN signer operators, five independent recovery operators, on-chain governance/HSM policy, monitoring, remaining development advisory resolution, live directional ULN302 validation and deployment remain.
- M2: user-approved funded one-way Sepolia → Arbitrum Sepolia deployment, independent DVN config, live event ingestion, real finalized GenLayer read.
- M3: listening mutual-TLS signer daemons backed by an official live GenLayer reader and five isolated operators/failure domains, HSM/KMS custody, monitoring, runbooks and deployed adversarial drills.
- M4: LayerZero/GenLayer confirmations resolved, external audit, design partner pilot. Mainnet is a separate decision.
