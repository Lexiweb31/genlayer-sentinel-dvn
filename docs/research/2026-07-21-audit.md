# Technical feasibility audit — 2026-07-21

Primary sources confirm both target testnets are LayerZero V2 deployments: Ethereum Sepolia chain/EID 11155111/40161 and Arbitrum Sepolia 421614/40231. Both official deployment pages list Endpoint V2 `0x6EDCE65403992e310A62460808c4b910D972f10f`. The current DVN interface assigns ULN302 jobs with destination EID, packet header, payload hash, confirmations and sender; the reference DVN verifies quorum-signed calls and tracks used hashes.

The official Gasolina API independently looks up `PacketSent`, enforces confirmations, and offers an extra-context boolean. It does not document pending or retry-after semantics. GenLayer documents the activation → proposal → commit → reveal → finalization pipeline, and warns that accepted results can change during appeal; external messages can execute only on finalized state. This supports the architecture, but the exact programmatic finality-consumption API remains to be confirmed.

Dependencies are intentionally minimal in M0. Solidity imports are local interfaces/implementations so source review is possible; production compilation against pinned official LayerZero/OApp packages is M1 after compatibility validation. No chain or cloud resource was created.

Sources: LayerZero deployment pages, DVN technical reference, Gasolina API reference, production DVN configuration guidance, LayerZero-v2 repository; GenLayer network configuration, messages, non-determinism, and Optimistic Democracy documentation.

## 2026-07-22 audit attempt

Fresh retrieval of the official LayerZero and GenLayer documentation was attempted before changing any address or network claim. Both the browser extractor and direct official-page downloads produced no usable content; the latter was rejected by the environment's network approval boundary. No address, pathway-support or Gasolina behavior claim was refreshed from that attempt.

Offline dependency consistency was verified from the installed tree and lockfile: `@layerzerolabs/lz-evm-protocol-v2` and `@layerzerolabs/lz-evm-messagelib-v2` are `3.0.168`, `@layerzerolabs/oapp-evm` is `0.4.1`, `genlayer-js` is `1.1.8`, and `ethers` is `6.17.0`. This proves local pinning only, not that those versions or `config/networks.json` addresses are current. A successful fresh official-source audit remains a deployment blocker.
