import type{RuntimeConfig}from"./runtime-config.js";import{SqliteJobStore}from"./job-store.js";import{SqliteListenerStore}from"./listener-store.js";import{IndependentRpcPacketVerifier}from"./rpc-verifier.js";import{GenLayerSdkFinality,type GenLayerClientFacade}from"./genlayer-finality.js";import{Coordinator}from"./coordinator.js";import{JsonRpcLogSource,PacketFeeListener}from"./listener.js";import{HttpsEvidenceSource,PolicyRequestFactory}from"./request-factory.js";import{coordinatorPacketHandler,IngestionRunner}from"./ingestion.js";import{createDashboardServer}from"./status-api.js";import{SentinelRuntime}from"./runtime.js";
import{SqliteRecoveryStore}from"./recovery-store.js";import{RecoveryService,recoveryFailurePolicy}from"./recovery-service.js";
export interface ComposedRuntime{runtime:SentinelRuntime;coordinator:Coordinator;recovery:RecoveryService;}
type DashboardServer=ReturnType<typeof createDashboardServer>;export interface SocketLifecycle{listen(server:DashboardServer,port:number,host:string):Promise<void>;close(server:DashboardServer):Promise<void>;}
const sockets:SocketLifecycle={listen:(server,port,host)=>new Promise((resolve,reject)=>{const fail=(error:Error)=>reject(error);server.once("error",fail);server.listen(port,host,()=>{server.off("error",fail);resolve()})}),close:server=>new Promise((resolve,reject)=>{if(!server.listening){resolve();return}server.close(error=>error?reject(error):resolve())})};
export function composeRuntime(config:RuntimeConfig,client:GenLayerClientFacade,dashboardRoot:string,report:(error:unknown)=>void=console.error,socket:SocketLifecycle=sockets):ComposedRuntime{const jobStore=new SqliteJobStore(config.storage.sqlitePath),listenerStore=new SqliteListenerStore(config.storage.sqlitePath),recoveryStore=new SqliteRecoveryStore(config.storage.sqlitePath),finality=new GenLayerSdkFinality(client,config.genlayer.policyContract),verifier=new IndependentRpcPacketVerifier(config.pathway.rpcUrls,config.pathway.endpoint,config.pathway.confirmations),coordinator=new Coordinator(verifier,finality,[],3,config.pathway.confirmations,jobStore),listener=new PacketFeeListener(new JsonRpcLogSource(config.pathway.rpcUrls[0]!),config.pathway.endpoint,config.pathway.sendLibrary,config.pathway.confirmations,config.pathway.startBlock,64n,listenerStore,config.pathway.name),factory=new PolicyRequestFactory({srcEid:config.pathway.srcEid,dstEid:config.pathway.dstEid,sender:config.pathway.sourceOApp,receiver:config.pathway.destinationOApp,sendLibrary:config.pathway.sendLibrary,sentinelDvn:config.pathway.sentinelDvn,evidenceUri:config.evidence.uri,policy:config.evidence.policy,evidenceTtlSeconds:config.evidence.ttlSeconds,maximumEvidenceBytes:config.evidence.maximumBytes},new HttpsEvidenceSource([config.evidence.allowedHost])),recovery=new RecoveryService(config.pathway.name,recoveryStore,listener),ingestion=new IngestionRunner(listener,coordinatorPacketHandler(factory,coordinator),recoveryFailurePolicy(config.pathway.name,recoveryStore,config.runtime.maxIngestionAttempts)),server=createDashboardServer(coordinator,dashboardRoot,recovery);const runtime=new SentinelRuntime({restore:()=>coordinator.restore(),ingest:async()=>{await ingestion.pollOnce()},pollFinality:async()=>{await coordinator.pollPending()},listen:()=>socket.listen(server,config.status.port,config.status.host),closeServer:()=>socket.close(server),closeStores:()=>{recoveryStore.close();listenerStore.close();jobStore.close()},report,intervalMs:config.runtime.pollIntervalMs});return{runtime,coordinator,recovery}}
