import test from"node:test";import assert from"node:assert/strict";import{SentinelRuntime}from"../../../dist/services/coordinator/src/runtime.js";
const deferred=()=>{let resolve;const promise=new Promise(done=>{resolve=done});return{promise,resolve}};
function setup(overrides={}){const calls=[],scheduled=[];let cancelled=0;const dependencies={restore:async()=>calls.push("restore"),ingest:async()=>calls.push("ingest"),pollFinality:async()=>calls.push("finality"),listen:async()=>calls.push("listen"),closeServer:async()=>calls.push("close-server"),closeStores:()=>calls.push("close-stores"),report:error=>calls.push(`error:${error.message}`),intervalMs:1000,schedule:task=>{scheduled.push(task);return()=>{cancelled++}},...overrides};return{runtime:new SentinelRuntime(dependencies),calls,scheduled,cancelled:()=>cancelled}}
test("restores and listens before scheduling serialized ingestion and finality",async()=>{const value=setup();await value.runtime.start();assert.deepEqual(value.calls,["restore","listen"]);assert.equal(value.scheduled.length,1);await value.scheduled[0]();assert.deepEqual(value.calls,["restore","listen","ingest","finality"]);assert.deepEqual(value.runtime.status,{started:true,stopping:false,tickActive:false})});
test("prevents overlapping ticks and reports errors without stopping",async()=>{const gate=deferred(),errors=[],value=setup({ingest:async()=>gate.promise,report:error=>errors.push(error.message)});await value.runtime.start();const first=value.scheduled[0](),second=value.scheduled[0]();assert.equal(value.runtime.status.tickActive,true);gate.resolve();await Promise.all([first,second]);assert.equal(value.calls.filter(call=>call==="finality").length,1);const failing=setup({ingest:async()=>{throw new Error("RPC down")},report:error=>errors.push(error.message)});await failing.runtime.start();await failing.scheduled[0]();assert.deepEqual(errors,["RPC down"]);assert.equal(failing.runtime.status.started,true)});
test("idempotent stop cancels scheduling, waits for a tick, then closes once",async()=>{const gate=deferred(),value=setup({ingest:async()=>gate.promise});await value.runtime.start();const tick=value.scheduled[0]();const stopping=value.runtime.stop();assert.deepEqual(value.calls,["restore","listen"]);gate.resolve();await tick;await stopping;await value.runtime.stop();assert.deepEqual(value.calls,["restore","listen","finality","close-server","close-stores"]);assert.equal(value.cancelled(),1);assert.deepEqual(value.runtime.status,{started:false,stopping:false,tickActive:false})});
