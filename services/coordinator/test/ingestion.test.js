import test from"node:test";import assert from"node:assert/strict";import{IngestionRunner}from"../../../dist/services/coordinator/src/ingestion.js";
const h=n=>`0x${n.repeat(64)}`,packet={transactionHash:h("1"),blockHash:h("2"),blockNumber:1n,encodedPayload:"0x",options:"0x",sendLibrary:"0x1111111111111111111111111111111111111111",requiredDvns:[],optionalDvns:[],fees:[]};
test("acknowledges only after the durable packet handler succeeds",async()=>{const calls=[],inbox={poll:async()=>[packet],acknowledge:async tx=>calls.push(["ack",tx])},runner=new IngestionRunner(inbox,async value=>calls.push(["handle",value.transactionHash]));assert.equal(await runner.pollOnce(),1);assert.deepEqual(calls,[["handle",h("1")],["ack",h("1")]])});
test("leaves a packet pending when the handler fails",async()=>{let acknowledged=false;const inbox={poll:async()=>[packet],acknowledge:async()=>{acknowledged=true}},runner=new IngestionRunner(inbox,async()=>{throw new Error("job store unavailable")});await assert.rejects(runner.pollOnce(),/job store unavailable/);assert.equal(acknowledged,false)});
