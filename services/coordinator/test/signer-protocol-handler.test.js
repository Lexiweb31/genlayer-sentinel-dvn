import test from"node:test";import assert from"node:assert/strict";import{Wallet,getBytes,verifyMessage}from"ethers";import{IsolatedSignerService,executionDigest}from"../../../dist/services/coordinator/src/signing.js";import{encodeSignerRequest,decodeSignerResponse}from"../../../dist/services/coordinator/src/signer-protocol.js";import{SignerProtocolHandler}from"../../../dist/services/coordinator/src/signer-protocol-handler.js";
const h=n=>`0x${n.repeat(64)}`,a=n=>`0x${n.repeat(40)}`,peer=h("f"),adapter=a("1"),target=a("2"),envelope={chainId:421614n,adapter,verificationTarget:target,guid:h("3"),packetDigest:h("4"),evidenceDigest:h("5"),callData:"0x1234",expiry:200n},result={guid:h("3"),packetDigest:h("4"),evidenceDigest:h("5"),decision:"ALLOW",reasonCode:"AUTHORIZED",finalizedAt:90,policyVersion:"v1"},request={version:"sentinel-signer/v1",requestId:h("a"),coordinatorId:"coordinator-west",issuedAt:100,expiresAt:130,envelope,result};
function fixture(options={}){const wallet=Wallet.createRandom(),service=new IsolatedSignerService({address:wallet.address,signMessageDigest:async digest=>wallet.signMessage(getBytes(digest))},{assertFinalized:options.assertFinalized??(async()=>{})},{chainId:421614n,adapter,verificationTarget:target,maxTtlSeconds:120n},()=>100n),store={reserve:options.reserve??(async()=>"RESERVED"),close:()=>{}},handler=new SignerProtocolHandler(service,store,{coordinatorId:"coordinator-west",coordinatorSpkiSha256:peer,maxRequestTtlSeconds:60},()=>100);return{wallet,handler}}
test("authenticates, reserves, independently validates, and signs a canonical request",async()=>{const{wallet,handler}=fixture(),reply=await handler.handle(peer,encodeSignerRequest(request)),response=decodeSignerResponse(reply.body);assert.equal(reply.status,200);assert.equal(response.requestId,request.requestId);assert.equal(response.signer,wallet.address.toLowerCase());assert.equal(response.digest,executionDigest(envelope));assert.equal(verifyMessage(getBytes(response.digest),response.signature).toLowerCase(),wallet.address.toLowerCase())});
test("fails closed on peer, coordinator, freshness, and lifetime",async()=>{const{handler}=fixture();for(const[authenticated,value,code]of[[h("e"),request,"AUTHENTICATION_FAILED"],[peer,{...request,coordinatorId:"coordinator-east"},"AUTHENTICATION_FAILED"],[peer,{...request,issuedAt:101,expiresAt:130},"INVALID_REQUEST"],[peer,{...request,issuedAt:90,expiresAt:99},"REQUEST_EXPIRED"],[peer,{...request,expiresAt:161},"INVALID_REQUEST"]]){const reply=await handler.handle(authenticated,encodeSignerRequest(value));assert.notEqual(reply.status,200);assert.equal(JSON.parse(reply.body).error.code,code)}});
test("maps replay, conflict, and signer refusal without leaking raw errors",async()=>{for(const[disposition,code]of[["DUPLICATE","REPLAYED_REQUEST"],["CONFLICT","CONFLICTING_REQUEST"]]){const{handler}=fixture({reserve:async()=>disposition}),reply=await handler.handle(peer,encodeSignerRequest(request));assert.equal(JSON.parse(reply.body).error.code,code)}const{handler}=fixture({assertFinalized:async()=>{throw new Error("secret upstream token")}}),reply=await handler.handle(peer,encodeSignerRequest(request));assert.equal(JSON.parse(reply.body).error.code,"SIGNING_REFUSED");assert.doesNotMatch(reply.body,/secret upstream token/)});
test("rejects malformed requests before signing",async()=>{const{handler}=fixture(),reply=await handler.handle(peer,"{broken");assert.equal(reply.status,400);assert.equal(JSON.parse(reply.body).error.code,"INVALID_REQUEST")});
