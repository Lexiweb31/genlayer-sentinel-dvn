import test from"node:test";import assert from"node:assert/strict";import{mkdtempSync,rmSync}from"node:fs";import{tmpdir}from"node:os";import{join}from"node:path";import{executionDigest}from"../../../dist/services/coordinator/src/signing.js";import{SqliteVerificationOutbox}from"../../../dist/services/coordinator/src/verification-outbox.js";import{DestinationWorker}from"../../../dist/services/coordinator/src/destination-worker.js";
const h=n=>`0x${n.repeat(64)}`,a=n=>`0x${n.repeat(40)}`,envelope={chainId:421614n,adapter:a("1"),verificationTarget:a("2"),guid:h("3"),packetDigest:h("4"),evidenceDigest:h("5"),callData:"0x1234",expiry:200n},digest=executionDigest(envelope),authorized=[a("6"),a("7"),a("8"),a("9"),a("a")],shares=[{address:a("6"),signature:`0x${"1".repeat(130)}`,digest},{address:a("7"),signature:`0x${"2".repeat(130)}`,digest},{address:a("8"),signature:`0x${"3".repeat(130)}`,digest}];
async function fixture(){const dir=mkdtempSync(join(tmpdir(),"sentinel-worker-")),store=new SqliteVerificationOutbox(join(dir,"state.db"),authorized,3);await store.plan(envelope.guid,envelope,100);await store.recordQuorum(envelope.guid,shares,100);return{dir,store,close(){store.close();rmSync(dir,{recursive:true,force:true})}}}
test("persists submission intent before broadcast and then the transaction hash",async()=>{const f=await fixture(),calls=[],adapter={used:async()=>false,submitVerification:async()=>{calls.push((await f.store.get(envelope.guid)).state);return h("a")}},worker=new DestinationWorker(f.store,adapter,{confirm:async()=>({status:"PENDING"})},{confirmExecution:async()=>{}},()=>110);assert.equal(await worker.pollOnce(),1);const record=await f.store.get(envelope.guid);assert.equal(record.state,"SUBMITTED");assert.equal(record.transactionHash,h("a"));assert.deepEqual(calls,["ATTEMPTING"]);f.close()});
test("waits for confirmation then advances destination execution",async()=>{const f=await fixture();await f.store.transition(envelope.guid,"READY",{state:"ATTEMPTING",updatedAt:105});await f.store.transition(envelope.guid,"ATTEMPTING",{state:"SUBMITTED",transactionHash:h("a"),updatedAt:106});const advanced=[],worker=new DestinationWorker(f.store,{used:async()=>true,submitVerification:async()=>{throw new Error("must not submit")}}, {confirm:async()=>({status:"CONFIRMED",confirmations:15n})},{confirmExecution:async guid=>advanced.push(guid)},()=>110);await worker.pollOnce();assert.equal((await f.store.get(envelope.guid)).state,"CONFIRMED");assert.deepEqual(advanced,[envelope.guid]);f.close()});
test("leaves pending receipts unchanged and records known failures",async()=>{for(const result of[{status:"PENDING"},{status:"FAILED",code:"EVENT_MISMATCH"}]){const f=await fixture();await f.store.transition(envelope.guid,"READY",{state:"ATTEMPTING",updatedAt:105});await f.store.transition(envelope.guid,"ATTEMPTING",{state:"SUBMITTED",transactionHash:h("a"),updatedAt:106});const worker=new DestinationWorker(f.store,{used:async()=>true,submitVerification:async()=>h("b")},{confirm:async()=>result},{confirmExecution:async()=>{}},()=>110);await worker.pollOnce();assert.equal((await f.store.get(envelope.guid)).state,result.status==="PENDING"?"SUBMITTED":"FAILED");f.close()}});
test("never rebroadcasts an ambiguous attempt or an already-used digest without receipt proof",async()=>{for(const setup of[async store=>store.transition(envelope.guid,"READY",{state:"ATTEMPTING",updatedAt:105}),async()=>{}]){const f=await fixture();await setup(f.store);let submissions=0;const worker=new DestinationWorker(f.store,{used:async()=>true,submitVerification:async()=>{submissions++;return h("a")}},{confirm:async()=>({status:"PENDING"})},{confirmExecution:async()=>{}},()=>110);await worker.pollOnce();assert.equal((await f.store.get(envelope.guid)).state,"RECOVERY_REQUIRED");assert.equal(submissions,0);f.close()}});
